# SQL Injection

SQL injection payloads help verify whether user input can change the structure
of database queries. Use payloads only against approved test environments.

## Boolean Based

```text
' OR '1'='1
```
